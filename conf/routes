# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
POST    /page/:num                              App.PagePosts
GET     /about                                  App.About
GET     /projects                               App.Projects

# Blog Post Routes
GET     /post/:id                               Post.Show
GET     /tag/:tag                               Post.Keywords
POST    /search                                 Post.Search

# User Sessions
GET     /login                                  Sessions.Index
GET     /logout                                 Sessions.Logout
POST    /auth/login                             Sessions.Login

# Management Section
GET     /manage                                 Manage.Index

# Post Management
GET     /manage/posts                           Posts.View
GET     /manage/posts/new                       Posts.New
GET     /manage/posts/edit/:id                  Posts.Edit

POST    /manage/posts/action/create             Posts.Create
POST    /manage/posts/action/edit               Posts.Modify

# Analytics
GET     /manage/analytics                       Analytics.View
GET     /manage/analytics/posts                 Analytics.Posts

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
